# Changesets

Add a changeset when you change the package: `bun run changeset`. Choose patch/minor/major and write a short summary. When the "Version Packages" PR is merged, the release workflow will publish to npm.
